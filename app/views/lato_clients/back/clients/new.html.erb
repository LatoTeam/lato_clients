<%

block = view(:layout, :block).new

back_button = view(:component, :button).new(title: 'All clients',
url: lato_clients.clients_path, size: :small)

actionbar = view(:layout, :actionbar).new(title: "New client",
widgets: [{component: back_button, align: :right}])

%>


<%=raw actionbar %>

<div class="content-row">
  <%=raw block.open %>

    <%= render 'lato_clients/back/clients/shared/form', method: :post %>

  <%=raw block.close %>
</div>
